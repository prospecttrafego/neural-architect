---
description: Create a React component following this pattern
---

1. Create a file in src/components/[name]/[Name].tsx Create file in src/components/[name]/[Name].tsx
2. Use TypeScript with a typed Props interface Use TypeScript with typed Props interface
3. Use Tailwind for styling Use Tailwind for styling
4. Export as named export
5. Create test file [Name].test.tsx